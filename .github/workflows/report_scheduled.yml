name: daily-report

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: print-message
        run: echo "Hello World from scheduled workflow"
